# Implementation Plans

Generated by the `/improve` skill on 2026-06-23 (commit `a3a4d2f`).

Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](./001-fix-engine-edge-cases.md) | Fix engine edge cases: gate timeout + getRun cache miss | P1 | S | — | ✅ DONE |
| [002](./002-render-design-system-content.md) | Render DesignSystem generated content in UI | P2 | S | — | ✅ DONE |
| [003](./003-env-example-and-docs.md) | .env.example + CLAUDE.md + integration & deployment docs | P2 | M | — | ✅ DONE |
| [004](./004-invoice-transfer-pipeline.md) | Invoice-based Floras transfer pipeline (3 use cases) | P1 | L | — | ✅ DONE |
| [005](./005-packages-ui-foundation.md) | Scaffold packages/ui shared component library | P2 | M | — | ✅ DONE |

Plans 001 and 002 are independent of each other and of 003–005 — run them in
parallel if you have two executors. Plan 004 (transfer pipeline) is
independent of 001–003 but adds new stages; if 001 lands first, 004 must
update `TRANSITIONS` to include gate timeout logic for the new stages too.

## Dependency notes

- **001 before 004 (if doing both)**: Plan 004 extends `TRANSITIONS` in `engine.ts`.
  If plan 001 has already added transfer stages to `STAGE_ORDER` and `WORK_STAGES`,
  plan 004's Step 6c should merge those additions rather than replace them.
- **005 before broader UI adoption**: Plan 005 establishes the `@floras/ui` package.
  Any future component refactors should import from `@floras/ui` rather than adding
  more inline styles.
- **003 before external integration**: The docs in plan 003 describe the
  `ExternalHttpAgent` contract. Other teams should read `docs/INTEGRATION.md`
  before attempting to integrate.

## Findings considered and rejected

- **No auth / RLS**: Real security gap, but TODOS.md already flags it explicitly.
  Not planned here because it requires Supabase Auth setup beyond code changes — needs a
  separate discussion about user identity model.
- **Zero test coverage**: Real gap. Not planned here because establishing test
  infrastructure requires choosing a test runner (vitest recommended for this stack)
  and writing characterization tests before any agent refactors. Defer to a dedicated
  "test infrastructure" plan when the team is ready.
- **packages/ui not adopted repo-wide**: Plan 005 starts the adoption in one
  component. Full adoption is incremental work across AgentCards, PipelineView,
  ApprovalDialog, etc. Not planned individually — follow the pattern from plan 005.

## About the plans

These plans were written against commit `a3a4d2f`. Run the drift-check command at
the top of each plan before executing to confirm the excerpts still match the live code.

If a plan is BLOCKED, record the reason in the Status column and open a GitHub issue
linking to the plan file.
